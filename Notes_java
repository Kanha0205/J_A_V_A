java is two types complie method both compile method avilable.
two type of structure to made a complete java, function and class.
Function structure is one type of method to create, This same function apply multiple times.
class is group of fuinctions, you could use diffrenttypes of functions.
void function is a no return type functions,
we would use java function, out of the classes, without class we code the function.
their are two types of class, The words first later are must be capital types of language sture we would use 1, pascal
pascal case convestion are used in first latter of the word are must be capital.
The camel case convestion are use each of word are must be capital, non of single value.
array ?
--  main reson use array it's fster to access,   array is shorcut variable that cantain huge data store ealsly, there benifis is, it's user friendly.
--- array is datatype colextion of simmiler type of data in multiple dimantion
--- arrya has unick loop called as a for each loop(fastest loop)
---  while array is a collection of data and if we use  loop also give a faster display
multyple Dimantion array?
----- one dimantion array values are only values.
---  multydimantion array means the arrays values are values with array, menase the values are convert to arrays array.
---- array starting with elements are stating wtih 0,1,2,3.......
----- what is method ?
method is used into a class, method is a type of rules set in class to follow.
mehods of java have a logical method, meanse the other class method implimante in currunt class.
method of java is not same as a class's functions.
there are two types of oprations 
1, static call
2, unstaating obj.call

-------------method Overl;oding
------- what is void ?
void means n retrun nothing.
void meaning the program print a code but they doesn't change, always same.
--------------what is static Keyword ?
sattic meanse stay, the code you are written in parent class, but call evey class child or other classes.
---------- what is the changing  intiger and , unchanging intiger.
changing intiger meanse without apply static code, you would change the value of every class value.
----------can we change the intiger value with static code ?
yes, with help of array you can manuplate the values of any array's adress, it could possible becaouse, array doesn't change
values.
-----------------What is extend means ?------
the extended meanse subclass call the  parantclass access their property's.
Super class is a constructor class, it call automaticly befor subclass.

----------------constructor---inheritance------------------
constructor inherite there method which was, he woude be created for con... inherite.
C1 - Parent
C2 - child
C3 - Grand child

-------method of overridng ?------
a child class applay the method to use parant class properties, is know as method of overriding.

-------------@overide?-----
override is a method, but @iverride method is a aware your class override or not.


-----------what is abstract class means?-------------
a small defination is a abstract class meanse at least one abstract codeline are avilable.
abstract class mean that class who work as a storage, ya modify lirarys.
abstract class are not use for oops, abstract class is basically use for create a more class in a java project.


-----------What is interfence ?..............
interface is group of methods.
interface meanse is face of code, in progranmning word. the interface is a outer layer of oops.
interface is help to coder useing the product.
  
-----------What is conection bitween abstract and interface in java ?..............
main diffrance interface properties appaly menedotry but, abstraction are not.

types of abrstract class are not creates non of the class, but interface creat a types of classes.








